#!/usr/bin/env bash
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
for f in "$ROOT"/tmp/*.pid; do
  [[ -f "$f" ]] || continue
  pid=$(cat "$f")
  if kill -0 "$pid" 2>/dev/null; then
    kill "$pid" 2>/dev/null || true
    echo "stopped $(basename "$f" .pid) ($pid)"
  fi
  rm -f "$f"
done
